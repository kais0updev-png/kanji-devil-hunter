# 🔧 「次へ→」ボタン問題のデバッグ対応

**実装日**: 2026-02-07  
**問題**: 「次へ→」ボタンをクリックしても次のカードが表示されない

---

## 🔍 実装した対策

### 1. デバッグログの追加

**目的**: 問題の原因を特定するため、詳細なログを追加

#### 追加したログ

**`startCardsSession()` 関数**:
```javascript
// キャッシュから読み込み時
console.log(`📦 キャッシュからカードデータ読み込み: ${cardsData.length}枚`);

// 新規取得時
console.log('🌐 Google Spreadsheetからカードデータ取得中...');
console.log(`📄 CSV取得完了: ${csv.length}文字`);
console.log(`📋 CSVから${lines.length}行を抽出`);
console.log(`📇 カード1:`, card); // 最初の3枚を表示

// セッション開始時
console.log(`🎴 カードセッション開始: ${cardsData.length}枚`);
```

**`goToNextCard()` 関数**:
```javascript
console.log(`➡️ 次のカードへ: ${currentCardIdx} → ${currentCardIdx + 1}`);
```

**`resetCardState()` 関数**:
```javascript
console.log('🔄 カード状態をリセット');
```

**`displayCurrentCard()` 関数**:
```javascript
console.log(`📇 カード表示: ${currentCardIdx + 1}/${cardsData.length} - ${card.word}`);
```

---

### 2. CSVパーサーの改善

**問題**: 単純な `split(',')` では、引用符やカンマを含むCSVを正しくパースできない

**改善内容**:
- より詳細なログ出力
- デフォルト値の設定（空欄対応）
- 最初の3枚のカードデータを出力してデバッグ

#### 改善前
```javascript
const lines = csv.split('\n').slice(1);
cardsData = lines.filter(l => l.trim()).map(l => {
  const parts = l.split(',');
  return { 
    id: parts[0], 
    word: parts[1], 
    hidden: parts[2], 
    reading: parts[3] || 'カナ',
    grade: parts[4], 
    category: parts[5] 
  };
});
```

#### 改善後
```javascript
const lines = csv.split('\n').slice(1).filter(l => l.trim());
console.log(`📋 CSVから${lines.length}行を抽出`);

cardsData = lines.map((l, index) => {
  const parts = l.split(',').map(p => p.trim());
  const card = { 
    id: parts[0] || `card-${index + 1}`, 
    word: parts[1] || '', 
    hidden: parts[2] || '', 
    reading: parts[3] || 'カナ',
    grade: parts[4] || '10', 
    category: parts[5] || 'unknown'
  };
  if (index < 3) {
    console.log(`📇 カード${index + 1}:`, card);
  }
  return card;
});
```

---

## 🔍 デバッグ手順

### ステップ1: ブラウザのコンソールを開く

1. ブラウザで F12 キーを押す
2. 「Console」タブを選択

### ステップ2: カードセッションを開始

1. CARDボタンをクリック
2. コンソールに以下のログが表示されることを確認：
   ```
   📦 キャッシュからカードデータ読み込み: N枚
   または
   🌐 Google Spreadsheetからカードデータ取得中...
   📄 CSV取得完了: XXXX文字
   📋 CSVからN行を抽出
   📇 カード1: {id: ..., word: ..., ...}
   📇 カード2: {id: ..., word: ..., ...}
   📇 カード3: {id: ..., word: ..., ...}
   
   🎴 カードセッション開始: N枚
   🔄 カード状態をリセット
   📇 カード表示: 1/N - XXX
   ```

### ステップ3: 「正解を見る」ボタンをクリック

コンソールに何も表示されない（これは正常）

### ステップ4: 「次へ→」ボタンをクリック

コンソールに以下のログが表示されることを確認：
```
➡️ 次のカードへ: 0 → 1
🔄 カード状態をリセット
📇 カード表示: 2/N - XXX
```

---

## 🐛 問題の可能性

### 可能性1: カードデータが空または1枚しかない

**確認方法**:
コンソールで以下のログを確認：
```
🎴 カードセッション開始: 0枚  ← 問題あり
または
🎴 カードセッション開始: 1枚  ← 問題あり（1枚しかない）
```

**解決方法**:
1. Admin画面 → 「問題データ更新」をクリック
2. LocalStorageをクリア：
   - F12 → Application → Local Storage → 右クリック → Clear
3. ページをリロード

### 可能性2: `goToNextCard()` が呼び出されていない

**確認方法**:
「次へ→」をクリックしても以下のログが表示されない：
```
➡️ 次のカードへ: X → Y
```

**解決方法**:
HTMLのボタンイベントを確認（すでに修正済み）

### 可能性3: CSVデータの形式が間違っている

**確認方法**:
コンソールで以下のログを確認：
```
📇 カード1: {id: undefined, word: undefined, ...}  ← 問題あり
```

**解決方法**:
Google Spreadsheetの「単語カード」シートを確認：
- 列順: `id, word, hiddenKanji, reading, grade, category`
- データが正しく入力されているか確認

---

## 🔧 緊急対策: キャッシュクリア

もし問題が続く場合、以下を実行してください：

### 方法1: Admin画面から

1. Admin画面を開く
2. 「問題データ更新」ボタンをクリック
3. 確認ダイアログで OK をクリック

### 方法2: ブラウザから直接

1. F12 → Console タブ
2. 以下のコマンドを入力して Enter:
   ```javascript
   localStorage.removeItem('kanjiCardsCache');
   localStorage.removeItem('kanjiCardsCacheTime');
   console.log('✅ カードキャッシュをクリアしました');
   ```
3. ページをリロード（F5）

---

## 📊 変更ファイル

| ファイル | 変更内容 | 変更量 |
|---------|---------|--------|
| `js/chainsaw-app.js` | デバッグログ追加 | +15行 |
| `js/chainsaw-app.js` | CSVパーサー改善 | +10行 |
| `KANJI_CARDS_DEBUG.md` | デバッグガイド | 新規作成 |

**合計変更量**: 約 **25行** 追加

---

## 📝 次のステップ

### ユーザーへのお願い

以下の情報を提供してください：

1. **ブラウザのコンソールログ**:
   - CARDボタンをクリック後のログ
   - 「次へ→」をクリック後のログ

2. **カードデータの件数**:
   - コンソールに表示される「カードセッション開始: N枚」の N の値

3. **エラーメッセージ**:
   - 赤いエラーメッセージがあれば、その内容

### 開発者向け

問題が特定されたら、以下を実装：
- より堅牢なCSVパーサー（引用符対応）
- エラーハンドリングの強化
- フォールバック処理の追加

---

**実装完了**: 2026-02-07  
**状態**: デバッグログ追加完了、ユーザーのフィードバック待ち  
**トークン使用量**: 87,082 / 200,000

---

**次のアクション**: ユーザーにブラウザのコンソールログを確認してもらい、問題を特定する
